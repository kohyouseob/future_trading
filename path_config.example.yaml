# 경로 환경 설정 예시 (이 파일을 path_config.yaml 로 복사 후 수정하세요)
# 다른 PC에서 실행 시 path_config.yaml 의 경로만 바꾸면 됩니다.
#
# mt5_path: MetaTrader 5 terminal64.exe 전체 경로 (필수)
# mt5_terminal_data_folder: (선택) MT5 데이터 폴더. 비우면 기본값 사용
# db_path: (선택) SQLite DB 경로. 비우면 v2/scheduler.db 사용

mt5_path: "C:\\Program Files\\MetaTrader 5\\terminal64.exe"
mt5_terminal_data_folder: ""
db_path: ""
